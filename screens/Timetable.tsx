import { Header, Calendar, Container } from "../components";

export const Timetable = () => {
    return (
        <Container>
            <Header />
            <Calendar />
        </Container>
    );
};
