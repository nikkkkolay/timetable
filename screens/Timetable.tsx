import { Header, Calendar, Container, ListTimetable } from "../components";

export const Timetable = () => {
    return (
        <Container>
            <Header />
            <ListTimetable />
        </Container>
    );
};
