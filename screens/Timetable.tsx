import { Header, Calendar, Container, ListDividers } from "../components";

export const Timetable = () => {
    return (
        <Container>
            <Header />
            <Calendar />
            <ListDividers />
        </Container>
    );
};
